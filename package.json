{
  "name": "@lapismd/terminal-host",
  "private": true,
  "version": "0.0.0",
  "description": "Standalone PTY process host and authenticated WebSocket server for LapisMD terminals",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "lapis-terminal-host": "./bin/lapis-terminal-host.mjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/bundle-cli.mjs",
    "serve:local": "tsx scripts/serve-local.mjs",
    "check": "pnpm spec:validate && pnpm check:types",
    "check:types": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "spec:build": "spec-validator build",
    "spec:check": "spec-validator check",
    "spec:first": "spec-validator first",
    "spec:validate": "spec-validator validate"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./client": {
      "types": "./dist/client.d.ts",
      "import": "./src/client.ts",
      "default": "./src/client.ts"
    },
    "./package.json": "./package.json"
  },
  "dependencies": {
    "node-pty": "^1.2.0-beta.13",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@lapismd/spec-validator": "link:../spec-validator",
    "@types/node": "^22.10.5",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.4",
    "tsx": "^4.23.1",
    "typescript": "~5.9.3",
    "vitest": "^4.1.8"
  }
}
